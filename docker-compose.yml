version: "3.9"

services:
  mermaid-lint:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      PARSER_SCRIPT: "/app/parser-node/parse.mjs"
    restart: unless-stopped
